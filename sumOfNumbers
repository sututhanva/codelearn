//https://codelearn.io/Training/Detail?Id=53109

function sumOfNumbers(arr, n){
    let sum=0;
    let newArr = [];
    for(let i=0;i<=n;i++){
        for(let j=0;j<arr.length;j++){
            if(i%arr[j]==0){
                newArr.push(i);
                break;
            }
        }
    }
    for(let i=0;i<newArr.length;i++){
        sum+=newArr[i];
    }
    return sum;
}
