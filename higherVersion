//https://codelearn.io/Training/Detail?Id=3924

function higherVersion(v1, v2){
    let arr1 = v1.split('.');
    let arr2 = v2.split('.');

    for(let i=0;i<arr1.length;i++){
        if(Number(arr1[i])>Number(arr2[i])){
            return true;
            break;
        }
        if(arr1[i]<arr2[i]) {
            return false;
        }
    }
}
